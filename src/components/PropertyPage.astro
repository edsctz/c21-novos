---
import type { PropertyConfig } from '../config/properties';
import Layout from '../layouts/Layout.astro';
import Header from './Header.astro';
import HeroSection from './property/HeroSection.astro';
import PropertySpecs from './property/PropertySpecs.astro';
import LeadForm from './LeadForm.astro';
import LeadFormSticky from './LeadFormSticky.jsx';
import Footer from './Footer.astro';

interface Props {
  config: PropertyConfig;
  content: any;
}

const { config, content } = Astro.props;
---

<Layout 
  title={config.seo.title}
  description={config.seo.description}
  gtmId={config.gtmId}
>
  <!-- Header Navigation -->
  <Header config={config} />
  
  <!-- Hero Section -->
  <HeroSection config={config} content={content.hero} />
  
  <!-- Property Specifications -->
  <PropertySpecs config={config} content={content.specs} />
  
  <!-- Lead Form Section -->
  <LeadForm 
    title={content.form.title}
    description={content.form.description}
    buttonText={content.form.cta}
    trackingLabel="Property Page Form Submission"
  />
  
  <!-- Neighborhood Section -->
  <section class="py-16 bg-digital-black">
    <div class="container">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">
          {content.location.title.split(' ').map((word, index) => 
            word === 'Privilegiada' ? <span class="text-gold">{word}</span> : word + ' '
          )}
        </h2>
        <p class="text-xl text-gray-300 max-w-3xl mx-auto">
          {content.location.description}
        </p>
      </div>
      
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 max-w-6xl mx-auto">
        <div>
          <h3 class="text-2xl font-bold text-gold mb-6">Proximidades</h3>
          <div class="space-y-4">
            {content.location.proximities.map((proximity) => (
              <div class="flex items-center">
                <div class="w-3 h-3 bg-relentless-gold rounded-full mr-4"></div>
                <span class="text-gray-300">{proximity}</span>
              </div>
            ))}
          </div>
          
          <div class="mt-8">
            <h4 class="text-lg font-semibold text-white mb-4">Valorização Imobiliária</h4>
            <p class="text-gray-300 text-sm leading-relaxed">
              Alphaville é reconhecida como a região de maior valorização imobiliária de Barueri, 
              com crescimento constante nos últimos 10 anos. Investir aqui é garantia de retorno.
            </p>
          </div>
        </div>
        
        <div class="bg-obsessed-grey rounded-2xl p-2 border border-digital-medium-grey">
          <img 
            src={config.images.location}
            alt={`Vista aérea de ${config.location} - Localização privilegiada`}
            class="w-full h-80 object-cover rounded-xl"
            loading="lazy"
          />
        </div>
      </div>
    </div>
  </section>
  
  <!-- CTA Final -->
  <section class="py-16 bg-gradient-gold">
    <div class="container text-center">
      <h2 class="text-3xl md:text-4xl font-bold text-digital-black mb-4">
        {content.finalCta.title}
      </h2>
      <p class="text-xl text-digital-black mb-8 max-w-3xl mx-auto">
        {content.finalCta.description}
      </p>
      
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <a 
          href={`https://wa.me/${config.contact.whatsapp}?text=${encodeURIComponent(content.finalCta.whatsappText)}`}
          target="_blank"
          rel="noopener noreferrer"
          class="bg-digital-black text-relentless-gold font-bold py-4 px-8 rounded-lg text-lg hover:bg-obsessed-grey transition-all duration-300 hover:scale-105 whatsapp-button"
          data-whatsapp-action="schedule_visit"
          data-whatsapp-location="final_cta"
        >
          📱 Agendar Visita via WhatsApp
        </a>
        
        <a 
          href={`tel:${config.contact.phone}`}
          class="bg-transparent border-2 border-digital-black text-digital-black font-bold py-4 px-8 rounded-lg text-lg hover:bg-digital-black hover:text-relentless-gold transition-all duration-300 hover:scale-105"
        >
          📞 Ligar Agora
        </a>
      </div>
    </div>
  </section>
  
  <!-- Sticky Lead Form (Mobile) -->
  <LeadFormSticky client:load />
  
  <!-- Footer -->
  <Footer config={config} />
</Layout>

<script>
  // GTM WhatsApp tracking for Property Page
  document.addEventListener('DOMContentLoaded', function() {
    const whatsappButtons = document.querySelectorAll('.whatsapp-button');
    
    whatsappButtons.forEach(button => {
      button.addEventListener('click', function(e) {
        const action = this.getAttribute('data-whatsapp-action') || 'click';
        const location = this.getAttribute('data-whatsapp-location') || 'unknown';
        
        // Fire GTM event
        if (typeof window.dataLayer !== 'undefined') {
          window.dataLayer.push({
            event: 'whatsapp_click',
            whatsapp_action: action,
            whatsapp_location: location,
            whatsapp_url: this.href
          });
        }
        
        // Also fire gtag event if available
        if (typeof gtag !== 'undefined') {
          gtag('event', 'whatsapp_click', {
            event_category: 'WhatsApp',
            event_label: `${location}_${action}`,
            value: 1
          });
        }
        
        console.log('WhatsApp click tracked:', {
          event: 'whatsapp_click',
          action: action,
          location: location,
          url: this.href
        });
      });
    });
  });
</script>
