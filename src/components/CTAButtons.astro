---
// CTA Buttons Component
---

<div class="cta-buttons-wrapper">
  <a 
    href="https://wa.me/5511990111592?text=Ol%C3%A1!%20Vi%20o%20site%20do%20lancamento%20em%20Alphaville%20e%20quero%20atendimento."
    target="_blank"
    rel="noopener noreferrer"
    class="cta-button cta-primary whatsapp-button"
    data-whatsapp-action="receive_price_table"
    data-whatsapp-location="cta_buttons"
  >
    📱 Receber Tabela de Preços
  </a>
  
  <a 
    href="tel:+5511990111592"
    class="cta-button cta-secondary"
  >
    📞 Ligar Agora
  </a>
</div>

<script>
  // GTM WhatsApp tracking for CTA buttons
  document.addEventListener('DOMContentLoaded', function() {
    const whatsappButtons = document.querySelectorAll('.whatsapp-button');
    
    whatsappButtons.forEach(button => {
      button.addEventListener('click', function(e) {
        const action = this.getAttribute('data-whatsapp-action') || 'click';
        const location = this.getAttribute('data-whatsapp-location') || 'unknown';
        
        // Fire GTM event
        if (typeof window.dataLayer !== 'undefined') {
          window.dataLayer.push({
            event: 'whatsapp_click',
            whatsapp_action: action,
            whatsapp_location: location,
            whatsapp_url: this.href
          });
        }
        
        // Also fire gtag event if available
        if (typeof gtag !== 'undefined') {
          gtag('event', 'whatsapp_click', {
            event_category: 'WhatsApp',
            event_label: `${location}_${action}`,
            value: 1
          });
        }
        
        console.log('WhatsApp click tracked:', {
          event: 'whatsapp_click',
          action: action,
          location: location,
          url: this.href
        });
      });
    });
  });
</script>
